{
    "$schema": "https://apify.com/schemas/v1/output.ide.json",
    "actorOutputSchemaVersion": 1,
    "title": "Businessfinder output",
    "properties": {
        "dataset": {
            "type": "string",
            "title": "Business leads",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "runSummary": {
            "type": "string",
            "title": "Run summary",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/RUN_SUMMARY"
        },
        "runReport": {
            "type": "string",
            "title": "Readable run report",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/RUN_REPORT"
        }
    }
}
